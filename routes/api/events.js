const express = require("express");
const router = express.Router();

// Load Event model
const Event = require("../../models/Event");

// @route GET api/events
// @desc load all the events
// @access Public

module.exports = router;
